# Analyze Vendors from deployed Adapters

This script analyzes and stores the vendors to each deployed adapter. The adapters are sourced from the euler oracle dashboard via the ./scrape-euler-adapters scripts.

This analysis does not reflect the actual usages of adapters, but the marketplace of available adapters vault creators can tap into.
